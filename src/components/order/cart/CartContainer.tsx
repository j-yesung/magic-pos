import React from 'react';
import useOrderStore from '@/shared/store/order';

const CartContainer = () => {
  // const { orderList } = useOrderStore();

  return <div>담은내역 확인</div>;
};

export default CartContainer;
