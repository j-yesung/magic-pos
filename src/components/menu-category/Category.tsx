const CategoryComponentPage = (props: { categories: CategoryType[] }) => {
  const { categories } = props;
  console.log(categories, 'categoriesaaa');
  // const categories = ['test1', 'test2', 'test3'];

  return <></>;
};

export default CategoryComponentPage;
